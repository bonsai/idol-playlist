name: idol-playlist-ci

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  gate:
    uses: ./.github/workflows/gate.yml

  ci:
    needs: gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "GATE passed; CI is authorized to continue."
